import React from 'react';

export default class ListFood extends React.Component{
    state={
       
    }

    render(){
        console.log(this.props); 
        return(
            <ul>

            </ul>
        )
    }
}